let share = document.getElementById('image-share');

share.addEventListener('click', () => {
    let navShare = document.getElementById('nav-share');
    let setDisplay = navShare.style.display = 'flex'
    return setDisplay
})
